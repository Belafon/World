<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.world">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.World">
        <activity android:name=".GameActivity.GameActivity"></activity>
        <activity
            android:name=".MenuScreen.WelcomingFragments.WelcomingActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".AbstractActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".MenuScreen.MenuActivity"
            android:screenOrientation="landscape" />
        <activity
            android:name=".MainActivity"
            android:screenOrientation="landscape">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>